const AddBookingsToQueue = async () => {
  try {
    // Write a cron job to add bookings to the queue whatever the logic you want
  } catch (error) {
    console.error('Error in AddBookingsToQueue:', error);
  }
};

module.exports = { AddBookingsToQueue };
